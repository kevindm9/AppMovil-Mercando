import { ListaSeleccionadaService } from './lista-seleccionada-service';

describe('ListaSeleccionadaService', () => {
  it('should create an instance', () => {
    expect(new ListaSeleccionadaService()).toBeTruthy();
  });
});
